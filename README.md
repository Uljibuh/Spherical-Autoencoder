# Spherical-Autoencoder
This repository contains a PyTorch implementation of a 3D Spherical Autoencoder trained on the MNIST dataset. Unlike standard autoencoders that rely on an unbounded Cartesian latent space, this model constrains the learned representations strictly to the surface of a 3D unit sphere.
